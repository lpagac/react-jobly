/** Renders form for updating user info
 * 
 * props:
 * - handleSubmit: function to update currUser in App state
 * - user: currUser
 * 
 * state: 
 * - formData: object like {firstName, lastName, email, password}
 * 
 */

function ProfileForm({handleSubmit, user}) {
  console.log("ProfileForm rendered");
  return (
    <div className="ProfileForm">
      ProfileForm
    </div>
  )
}

export default ProfileForm;